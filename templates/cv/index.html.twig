{% extends 'base.html.twig' %}

{% block title %}Hello CVController!{% endblock %}

{% block body %}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nicolas Cataluna - Développeur Fullstack</title>
    <style>
        :root {
            --bg-primary: #000000;
            --bg-secondary: #0a0a0a;
            --bg-tertiary: #000000;
            --bg-card: rgba(20, 20, 20, 0.9);
            --bg-card-hover: rgba(255, 255, 255, 0.05);
            --text-primary: #e0e0e0;
            --text-secondary: #b0b0b0;
            --accent-primary: #6495ed;
            --accent-secondary: #87ceeb;
            --accent-tertiary: #4169e1;
            --border-color: rgba(100, 149, 237, 0.3);
            --shadow-color: rgba(100, 149, 237, 0.2);
            --tag-bg: rgba(100, 149, 237, 0.2);
            --success-color: #90ee90;
            --success-bg: rgba(50, 205, 50, 0.2);
            --success-border: rgba(50, 205, 50, 0.5);
            --star-color: white;
            --planet-opacity: 0.3;
        }

        [data-theme="light"] {
            --bg-primary: #ffffff;
            --bg-secondary: #f8f9fa;
            --bg-tertiary: #ffffff;
            --bg-card: rgba(255, 255, 255, 0.95);
            --bg-card-hover: rgba(100, 149, 237, 0.08);
            --text-primary: #1a1a1a;
            --text-secondary: #4a5568;
            --accent-primary: #2563eb;
            --accent-secondary: #1e40af;
            --accent-tertiary: #3b82f6;
            --border-color: rgba(37, 99, 235, 0.3);
            --shadow-color: rgba(37, 99, 235, 0.15);
            --tag-bg: rgba(37, 99, 235, 0.15);
            --success-color: #059669;
            --success-bg: rgba(16, 185, 129, 0.15);
            --success-border: rgba(16, 185, 129, 0.4);
            --star-color: #fbbf24;
            --planet-opacity: 0.15;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: var(--bg-primary);
            color: var(--text-primary);
            min-height: 100vh;
            overflow-x: hidden;
            position: relative;
            transition: background 0.3s ease, color 0.3s ease;
        }

        /* Toggle Theme Button */
        .theme-toggle {
            position: fixed;
            top: 90px;
            right: 20px;
            z-index: 1001;
            background: var(--bg-card);
            border: 2px solid var(--border-color);
            border-radius: 50px;
            padding: 12px 20px;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 10px;
            box-shadow: 0 4px 15px var(--shadow-color);
            transition: all 0.3s ease;
        }

        .theme-toggle:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px var(--shadow-color);
        }

        .theme-toggle-icon {
            font-size: 1.2em;
            /* Remplacez par ux:sun ou ux:moon selon le thème */
        }

        .theme-toggle-text {
            font-weight: 600;
            color: var(--accent-primary);
        }

        /* Étoiles animées */
        .stars {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
        }

        .star {
            position: absolute;
            width: 2px;
            height: 2px;
            background: var(--star-color);
            border-radius: 50%;
            animation: twinkle 3s infinite;
            transition: background 0.3s ease;
        }

        @keyframes twinkle {
            0%, 100% { opacity: 0.3; }
            50% { opacity: 1; }
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 40px 20px;
            position: relative;
            z-index: 1;
        }

        /* Header */
        .header {
            text-align: center;
            padding: 60px 20px;
            background: var(--bg-card);
            border-radius: 20px;
            border: 2px solid var(--border-color);
            box-shadow: 0 0 40px var(--shadow-color);
            margin-bottom: 40px;
            margin-top: 60px;
            position: relative;
            overflow: hidden;
            transition: all 0.3s ease;
        }

        .header::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, var(--shadow-color) 0%, transparent 70%);
            animation: rotate 20s linear infinite;
        }

        @keyframes rotate {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .header-content {
            position: relative;
            z-index: 1;
        }

        h1 {
            font-size: 3.5em;
            margin-bottom: 10px;
            background: linear-gradient(135deg, var(--accent-primary), var(--accent-tertiary), var(--accent-secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            filter: drop-shadow(0 0 20px var(--shadow-color));
        }

        .subtitle {
            font-size: 1.5em;
            color: var(--accent-secondary);
            margin-bottom: 20px;
        }

        .tags {
            display: flex;
            gap: 15px;
            justify-content: center;
            flex-wrap: wrap;
            margin-top: 20px;
        }

        .tag {
            background: var(--tag-bg);
            padding: 8px 20px;
            border-radius: 20px;
            border: 1px solid var(--border-color);
            font-size: 0.9em;
            color: var(--accent-secondary);
            transition: all 0.3s ease;
        }

        .tag:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 10px var(--shadow-color);
        }

        .contact-info {
            display: flex;
            gap: 30px;
            justify-content: center;
            flex-wrap: wrap;
            margin-top: 30px;
        }

        .contact-item {
            display: flex;
            align-items: center;
            gap: 10px;
            color: var(--text-secondary);
            text-decoration: none;
            transition: all 0.3s;
        }

        .contact-item:hover {
            color: var(--accent-primary);
            transform: translateY(-2px);
        }

        .contact-icon {
            font-size: 1.2em;
            /* Remplacez par les icônes UX appropriées */
        }

        /* Sections */
        .section {
            background: var(--bg-card);
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            border: 1px solid var(--border-color);
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }

        .section:hover {
            border-color: var(--accent-primary);
            box-shadow: 0 0 30px var(--shadow-color);
        }

        .section-title {
            font-size: 2em;
            margin-bottom: 25px;
            color: var(--accent-primary);
            border-bottom: 2px solid var(--border-color);
            padding-bottom: 10px;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .section-icon {
            font-size: 1.2em;
            /* Remplacez par les icônes UX appropriées */
        }

        /* Expériences */
        .experience-item {
            margin-bottom: 30px;
            padding: 20px;
            background: var(--bg-card-hover);
            border-radius: 10px;
            border-left: 4px solid var(--accent-primary);
            transition: all 0.3s ease;
        }

        .experience-item:hover {
            background: var(--tag-bg);
            transform: translateX(5px);
        }

        .experience-header {
            display: flex;
            justify-content: space-between;
            align-items: start;
            margin-bottom: 15px;
            flex-wrap: wrap;
            gap: 10px;
        }

        .experience-title {
            font-size: 1.3em;
            color: var(--accent-secondary);
            font-weight: bold;
        }

        .experience-company {
            color: var(--accent-primary);
            font-weight: 600;
            margin-top: 5px;
        }

        .experience-period {
            color: var(--text-secondary);
            font-size: 0.9em;
            white-space: nowrap;
        }

        .experience-description {
            list-style: none;
            margin-top: 15px;
        }

        .experience-description li {
            padding-left: 25px;
            margin-bottom: 10px;
            position: relative;
            line-height: 1.6;
            color: var(--text-primary);
        }

        .experience-description li::before {
            content: '▸';
            position: absolute;
            left: 0;
            color: var(--accent-primary);
            font-weight: bold;
        }

        /* Compétences */
        .skills-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
        }

        .skill-category {
            background: var(--bg-card-hover);
            padding: 20px;
            border-radius: 10px;
            border: 1px solid var(--border-color);
            transition: all 0.3s ease;
        }

        .skill-category:hover {
            border-color: var(--accent-primary);
            transform: translateY(-3px);
            box-shadow: 0 5px 20px var(--shadow-color);
        }

        .skill-category-title {
            color: var(--accent-secondary);
            font-size: 1.2em;
            margin-bottom: 15px;
            font-weight: bold;
        }

        .skill-list {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }

        .skill-item {
            background: var(--tag-bg);
            padding: 6px 15px;
            border-radius: 15px;
            font-size: 0.9em;
            border: 1px solid var(--border-color);
            transition: all 0.3s;
            color: var(--text-primary);
        }

        .skill-item:hover {
            background: var(--accent-primary);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px var(--shadow-color);
        }

        /* Formations */
        .formation-item {
            margin-bottom: 25px;
            padding: 20px;
            background: var(--bg-card-hover);
            border-radius: 10px;
            border-left: 4px solid var(--accent-tertiary);
            transition: all 0.3s ease;
        }

        .formation-item:hover {
            background: var(--tag-bg);
            transform: translateX(5px);
        }

        .formation-title {
            font-size: 1.2em;
            color: var(--accent-secondary);
            margin-bottom: 8px;
            font-weight: bold;
        }

        .formation-school {
            color: var(--accent-primary);
            margin-bottom: 5px;
        }

        .formation-period {
            color: var(--text-secondary);
            font-size: 0.9em;
        }

        .formation-status {
            display: inline-block;
            margin-top: 10px;
            padding: 5px 15px;
            background: var(--success-bg);
            border: 1px solid var(--success-border);
            border-radius: 15px;
            color: var(--success-color);
            font-size: 0.85em;
            font-weight: 600;
        }

        /* Langues */
        .languages-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
        }

        .language-item {
            background: var(--bg-card-hover);
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            border: 1px solid var(--border-color);
            transition: all 0.3s ease;
        }

        .language-item:hover {
            border-color: var(--accent-primary);
            transform: translateY(-3px);
            box-shadow: 0 5px 15px var(--shadow-color);
        }

        .language-name {
            font-size: 1.2em;
            color: var(--accent-secondary);
            margin-bottom: 10px;
            font-weight: bold;
        }

        .language-level {
            color: var(--text-secondary);
        }

        /* Centres d'intérêt */
        .interests-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 15px;
        }

        .interest-item {
            background: var(--bg-card-hover);
            padding: 15px 20px;
            border-radius: 10px;
            border-left: 3px solid var(--accent-primary);
            transition: all 0.3s;
            color: var(--text-primary);
        }

        .interest-item:hover {
            background: var(--tag-bg);
            transform: translateX(5px);
        }

        /* Effet planète */
        .planet {
            position: fixed;
            width: 200px;
            height: 200px;
            border-radius: 50%;
            background: radial-gradient(circle at 30% 30%, var(--accent-tertiary), var(--bg-secondary));
            box-shadow: 0 0 60px var(--shadow-color), inset -20px -20px 50px rgba(0, 0, 0, 0.5);
            bottom: -100px;
            right: -100px;
            z-index: 0;
            opacity: var(--planet-opacity);
            transition: all 0.3s ease;
        }

        /* Responsive */
        @media (max-width: 768px) {
            h1 {
                font-size: 2.5em;
            }
            
            .subtitle {
                font-size: 1.2em;
            }

            .contact-info {
                flex-direction: column;
                align-items: center;
            }

            .experience-header {
                flex-direction: column;
            }

            .skills-grid {
                grid-template-columns: 1fr;
            }

            .theme-toggle {
                top: 80px;
                right: 10px;
                padding: 10px 15px;
            }
        }

        @media print {
            .theme-toggle,
            .stars,
            .planet {
                display: none;
            }

            body {
                background: white;
            }

            .section {
                page-break-inside: avoid;
            }
        }
    </style>
</head>
<body>
    <!-- Toggle Theme Button -->
    <button class="theme-toggle" id="themeToggle" aria-label="Changer le thème">
        <span class="theme-toggle-icon" id="themeIcon">
            <span id="iconSun" style="display: none;">{{ ux_icon('bi:sun-fill', {'height': '20px', 'width': '20px'}) }}</span>
            <span id="iconMoon">{{ ux_icon('bi:moon-fill', {'height': '20px', 'width': '20px'}) }}</span>
        </span>
        <span class="theme-toggle-text" id="themeText">Mode Clair</span>
    </button>

    <div class="stars" id="stars"></div>
    <div class="planet"></div>

    <div class="container">
        <!-- Header -->
        <div class="header">
            <div class="header-content">
                <h1>NICOLAS CATALUNA</h1>
                <div class="subtitle">Développeur Fullstack</div>
                
                <div class="tags">
                    <span class="tag">Curieux</span>
                    <span class="tag">Rigoureux</span>
                    <span class="tag">Autonome</span>
                </div>

                <div class="contact-info">
                    <a href="tel:+33767807469" class="contact-item">
                        <span class="contact-icon">{{ ux_icon('bi:telephone-fill', {'height': '20px', 'width': '20px'}) }}</span>
                        +33 7 67 80 74 69
                    </a>
                    <a href="mailto:nicolas.cataluna@proton.me" class="contact-item">
                        <span class="contact-icon">{{ ux_icon('bi:envelope-fill', {'height': '20px', 'width': '20px'}) }}</span>
                        nicolas.cataluna@proton.me
                    </a>
                    <a href="https://github.com/nicoopo" target="_blank" class="contact-item">
                        <span class="contact-icon">{{ ux_icon('iconoir:github', {'height': '20px', 'width': '20px'}) }}</span>
                        GitHub
                    </a>
                    <a href="https://nicolascataluna.fr" target="_blank" class="contact-item">
                        <span class="contact-icon">{{ ux_icon('bi:globe', {'height': '20px', 'width': '20px'}) }}</span>
                        Portfolio
                    </a>
                    <a href="https://www.linkedin.com/in/nicolas-cataluna-737182224/" target="_blank" class="contact-item">
                        <span class="contact-icon">{{ ux_icon('streamline-pixel:logo-linkedin', {'height': '20px', 'width': '20px'}) }}</span>
                        LinkedIn
                    </a>
                </div>
            </div>
        </div>

        <!-- Profil -->
        <div class="section">
            <h2 class="section-title">
                <span class="section-icon">{{ ux_icon('bi:person-fill', {'height': '24px', 'width': '24px'}) }}</span>
                Profil
            </h2>
            <p style="line-height: 1.8;">
                Développeur full-stack junior spécialisé Symfony, motivé par la conception de solutions web performantes et les environnements connectés (domotique). Curieux, rigoureux et passionné par les nouvelles technologies, je cherche à contribuer à des projets concrets en lien avec l'innovation digitale.
            </p>
            <p style="margin-top: 15px; color: var(--accent-secondary); font-weight: bold;">
                🎓 En recherche d'alternance : Bachelor IA, Développement Fullstack DevOps chez IPSSI<br>
                📅 Rythme : 3 semaines en entreprise / 1 semaine en formation (12 mois)
            </p>
        </div>

        <!-- Expériences -->
        <div class="section">
            <h2 class="section-title">
                <span class="section-icon">{{ ux_icon('bi:briefcase-fill', {'height': '24px', 'width': '24px'}) }}</span>
                Expériences Professionnelles
            </h2>

            <div class="experience-item">
                <div class="experience-header">
                    <div>
                        <div class="experience-title">Développeur</div>
                        <div class="experience-company">TESSI ENCAISSEMENTS | NANTERRE</div>
                    </div>
                    <div class="experience-period">2023 - 2024 | Contrat d'apprentissage</div>
                </div>
                <ul class="experience-description">
                    <li>Migration de sites web d'un noyau PHP vers Symfony (domaine de l'encaissement)</li>
                    <li>Mise en place de composants dans Symfony : export de données, création de graphiques dynamiques, tableau de données dynamique</li>
                    <li>Projet évolution d'un nouveau noyau web - collaboration au développement</li>
                </ul>
            </div>

            <div class="experience-item">
                <div class="experience-header">
                    <div>
                        <div class="experience-title">Stagiaire Technicien Informatique</div>
                        <div class="experience-company">TESSI ENCAISSEMENTS | NANTERRE</div>
                    </div>
                    <div class="experience-period">Mai - Juin 2022 | Stage</div>
                </div>
                <ul class="experience-description">
                    <li>Création d'un support Excel répertoriant l'ensemble des serveurs (vérification obsolescence et état matériel)</li>
                    <li>Mise en place de flux de transfert et traitement de données</li>
                    <li>Paramétrage de l'application domotique DOMOTICZ</li>
                    <li>Implémentation de protocoles Wifi et Bluetooth (IPV6, domotique)</li>
                </ul>
            </div>
        </div>

        <!-- Compétences -->
        <div class="section">
            <h2 class="section-title">
                <span class="section-icon">{{ ux_icon('bi:code-slash', {'height': '24px', 'width': '24px'}) }}</span>
                Compétences
            </h2>
            <div class="skills-grid">
                <div class="skill-category">
                    <div class="skill-category-title">Développement Fullstack & Mobile</div>
                    <div class="skill-list">
                        <span class="skill-item">PHP (OOP)</span>
                        <span class="skill-item">Symfony</span>
                        <span class="skill-item">MVC</span>
                        <span class="skill-item">HTML/CSS/JS</span>
                        <span class="skill-item">JavaScript</span>
                        <span class="skill-item">Java</span>
                    </div>
                </div>

                <div class="skill-category">
                    <div class="skill-category-title">BDD & Hébergement</div>
                    <div class="skill-list">
                        <span class="skill-item">MySQL</span>
                        <span class="skill-item">MariaDB</span>
                        <span class="skill-item">PostgreSQL</span>
                        <span class="skill-item">SQL Server</span>
                        <span class="skill-item">VMWare</span>
                    </div>
                </div>

                <div class="skill-category">
                    <div class="skill-category-title">Sécurité, Réseaux & Télécom</div>
                    <div class="skill-list">
                        <span class="skill-item">Cisco</span>
                        <span class="skill-item">802.1x</span>
                        <span class="skill-item">Switch/Router</span>
                        <span class="skill-item">Protocoles domotiques</span>
                        <span class="skill-item">DOMOTICZ</span>
                    </div>
                </div>

                <div class="skill-category">
                    <div class="skill-category-title">Outils & DevOps</div>
                    <div class="skill-list">
                        <span class="skill-item">Git / GitHub</span>
                        <span class="skill-item">GitLab CI/CD</span>
                        <span class="skill-item">Windows/Linux</span>
                        <span class="skill-item">Ubuntu</span>
                        <span class="skill-item">Debian</span>
                        <span class="skill-item">Bash/PowerShell</span>
                    </div>
                </div>

                <div class="skill-category">
                    <div class="skill-category-title">Informatique Générale</div>
                    <div class="skill-list">
                        <span class="skill-item">Pack Office</span>
                        <span class="skill-item">SST/PRAP</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Formations -->
        <div class="section">
            <h2 class="section-title">
                <span class="section-icon">{{ ux_icon('bi:mortarboard-fill', {'height': '24px', 'width': '24px'}) }}</span>
                Formations
            </h2>

            <div class="formation-item">
                <div class="formation-title">Bachelor IA, Développement Fullstack DevOps</div>
                <div class="formation-school">IPSSI</div>
                <div class="formation-period">2025 - 2026</div>
                <span class="formation-status">En cours</span>
            </div>

            <div class="formation-item">
                <div class="formation-title">BTS Services Informatiques aux Organisations - SLAM</div>
                <div class="formation-school">ROBERT SCHUMAN | DUGNY</div>
                <div class="formation-period">2023 - 2024</div>
                <span class="formation-status">Obtenu</span>
            </div>

            <div class="formation-item">
                <div class="formation-title">Baccalauréat Professionnel Systèmes Numériques - RISC</div>
                <div class="formation-school">LA SALLE | SAINT-DENIS (93)</div>
                <div class="formation-period">2021 - 2022</div>
                <span class="formation-status">Obtenu avec mention Assez Bien</span>
            </div>

            <div class="formation-item">
                <div class="formation-title">Brevet des Collèges</div>
                <div class="formation-school">LA SALLE | SAINT-DENIS (93)</div>
                <div class="formation-period">2018 - 2019</div>
                <span class="formation-status">Obtenu</span>
            </div>
        </div>

        <!-- Langues -->
        <div class="section">
            <h2 class="section-title">
                <span class="section-icon">{{ ux_icon('bi:translate', {'height': '24px', 'width': '24px'}) }}</span>
                Langues
            </h2>
            <div class="languages-grid">
                <div class="language-item">
                    <div class="language-name">Français</div>
                    <div class="language-level">Langue maternelle</div>
                </div>
                <div class="language-item">
                    <div class="language-name">Portugais</div>
                    <div class="language-level">Courant</div>
                </div>
                <div class="language-item">
                    <div class="language-name">Anglais</div>
                    <div class="language-level">B1</div>
                </div>
                <div class="language-item">
                    <div class="language-name">Espagnol</div>
                    <div class="language-level">A2</div>
                </div>
            </div>
        </div>

        <!-- Centres d'intérêt -->
        <div class="section">
            <h2 class="section-title">
                <span class="section-icon">{{ ux_icon('bi:star-fill', {'height': '24px', 'width': '24px'}) }}</span>
                Centres d'Intérêt
            </h2>
            <div class="interests-grid">
                <div class="interest-item">
                    🎵 Musique Latino, R&B, Création musicale assistée par ordinateur (MAO)
                </div>
                <div class="interest-item">
                    ✈️ Voyages : Portugal, Espagne, France et découverte de cultures
                </div>
                <div class="interest-item">
                    💪 Sport : Musculation, Basketball
                </div>
                <div class="interest-item">
                    🔭 Physique, Astronomie
                </div>
            </div>
        </div>
    </div>

    <script>
        // Génération des étoiles
        const starsContainer = document.getElementById('stars');
        const numberOfStars = 100;

        for (let i = 0; i < numberOfStars; i++) {
            const star = document.createElement('div');
            star.className = 'star';
            star.style.left = Math.random() * 100 + '%';
            star.style.top = Math.random() * 100 + '%';
            star.style.animationDelay = Math.random() * 3 + 's';
            star.style.opacity = Math.random() * 0.7 + 0.3;
            starsContainer.appendChild(star);
        }

        // Effet parallaxe au scroll
        window.addEventListener('scroll', () => {
            const scrolled = window.pageYOffset;
            const stars = document.querySelectorAll('.star');
            stars.forEach((star, index) => {
                const speed = (index % 3 + 1) * 0.1;
                star.style.transform = `translateY(${scrolled * speed}px)`;
            });
        });

        // Gestion du thème
        const themeToggle = document.getElementById('themeToggle');
        const themeIcon = document.getElementById('themeIcon');
        const themeText = document.getElementById('themeText');
        const html = document.documentElement;

        // Charger le thème sauvegardé ou utiliser le thème sombre par défaut
        const savedTheme = localStorage.getItem('theme') || 'dark';
        html.setAttribute('data-theme', savedTheme);
        updateThemeButton(savedTheme);

        // Toggle du thème
        themeToggle.addEventListener('click', () => {
            const currentTheme = html.getAttribute('data-theme');
            const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
            
            html.setAttribute('data-theme', newTheme);
            localStorage.setItem('theme', newTheme);
            updateThemeButton(newTheme);
        });

        function updateThemeButton(theme) {
            const iconSun = document.getElementById('iconSun');
            const iconMoon = document.getElementById('iconMoon');
            if (theme === 'dark') {
                iconSun.style.display = 'none';
                iconMoon.style.display = 'inline';
                themeText.textContent = 'Mode Clair';
            } else {
                iconSun.style.display = 'inline';
                iconMoon.style.display = 'none';
                themeText.textContent = 'Mode Sombre';
            }
        }
    </script>
</body>
</html>
{% endblock %}
